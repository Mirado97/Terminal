# ═══════════════════════════════════════════════════════
#  Горячий конфиг — редактируй на ходу, перезагрузка 5с
#  Бот на паузе (P) → nano config.py → сохранить → готово
# ═══════════════════════════════════════════════════════

# Минимальный executable спред для входа в позицию (bps)
ENTRY_THRESHOLD = 200.0

# Порог выхода: если спред упал ниже — выходим (bps)
EXIT_THRESHOLD = -2.0

# Выход когда от входного спреда осталось ≤ этой доли (80% захвачено)
DYNAMIC_EXIT_RATIO = 0.20

# Принудительный выход через N секунд если позиция не закрылась
MAX_HOLD_S = 60

# Максимальное количество одновременных открытых позиций
MAX_POSITIONS = 3

# Кулдаун после принудительного закрытия по тайм-ауту (секунды)
COOLDOWN_S = 1800

# Размер виртуальной позиции на одну сторону (USDT)
VIRTUAL_SIZE_USDT = 50.0

# Минимальный интервал между входами (секунды) — глобальный лимит сделок
MIN_ENTRY_INTERVAL_S = 60.0

# Минимальный суточный объём пары на MEXC Spot (USDT) — фильтр неликвида
MIN_MEXC_VOLUME_USDT = 500_000
